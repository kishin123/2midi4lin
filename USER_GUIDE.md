# 2midi4lin 使用说明

> 把任意音频变成钢琴独奏 MIDI 的桌面工具，适配米哈游桌面程序「林离」。
> 本文面向普通用户，跟着步骤走即可。

---

## 1. 下载与安装

1. 从 GitHub Release 页面下载 `2midi4lin.exe`
2. 双击运行，**无需安装**，打开即用
3. 首次使用建议把 exe 放到一个固定文件夹（产物默认保存在 exe 同目录）

> 提示：若杀毒软件误报，属常见误报（PyInstaller 打包的程序），添加信任即可。

## 2. 主界面

启动后是单窗口应用，顶部两个功能页签：

```
[🎹 转录]  [📥 下载]
```

- **转录页**：上传音频 / 视频链接 → 生成 MIDI → 分享
- **下载页**：搜索或下载现成的 MIDI 钢琴谱

## 3. 功能一：音频转录（核心）

把一首歌变成钢琴独奏 MIDI：

1. 点击「选择文件」或直接把音频文件**拖进窗口**（支持 mp3/wav/flac/m4a 等）
2. 选择**转录模式**：
   - **忠实转录**（AMT）：严格还原原曲实际弹奏的音符，适合钢琴独奏/纯钢琴视频
   - **翻奏改编**（APC）：按钢琴风格重新编排，适合流行歌、混音原曲
3. 选择演奏风格：轻柔 / 标准 / 华丽
4. 点击「开始转录」，等待进度条完成
5. 完成后自动弹出**分享窗**，同时可在「打开文件夹」查看生成的 `.mid` 文件

> 小技巧：转录前可先点「分析」，软件会评估这首音频的钢琴转录适配度（1-5 星）。

## 4. 功能二：视频转 MIDI

直接把钢琴演奏视频变成 MIDI：

1. 转录页选择「视频链接」模式
2. 粘贴 **B 站** 或 **YouTube** 视频地址
3. 点击「开始」，自动完成：下载音频 → 转换格式 → 转录
4. 完成后同样弹出分享窗

**常见问题**：
- YouTube 报 `Sign in to confirm you're not a bot` → 换 B 站链接，或按 [FAQ.md](FAQ.md) 第 1 条配置 cookies
- 视频被删/私密/地区限制 → 换一个视频

> 视频链接与本地文件二选一（互斥），同时只能用一个来源。

## 5. 功能三：下载钢琴谱

在下载页直接搜索现成 MIDI：

1. 输入歌名（支持中英文），回车搜索
2. 结果**边搜边显示**（先到的先展示），来自多个 MIDI 资源站
3. 点击目标结果的「下载」，保存到 `downloads` 文件夹
4. 也可以粘贴 **MuseScore 乐谱链接** 直接下载

> 没搜到？页面底部有一排资源站链接（MidiShow / FreeMIDI / Piano-Midi 等），可手动浏览。

## 6. 分享作品（林离）

生成的 MIDI 可以分享到作品集网站（2midi4lin.kesug.com），流程：

1. 转录/视频完成后弹出分享窗
2. 打开「📂 打开成品文件夹」，找到生成的 `.mid` 文件
3. 打开米哈游**林离**桌面程序，加载这个 MIDI，在播放页面能看到**分享码**（形如 `L35X0G`）
4. 把分享码填回分享窗 → 点击「提交分享」
5. 作品即展示在作品集：[2midi4lin.kesug.com](https://2midi4lin.kesug.com)

> 右上角随时可以点「📤」分享，会自动带上最后一次生成的成品文件名。

## 7. 保存目录

产物统一保存在 exe 同目录的三个子文件夹：

```
exe 同目录/
├── transcribe/   ← 音频转录的 MIDI
├── videos/       ← 视频转 MIDI 的产物
└── downloads/    ← 搜索下载的 MIDI
```

- 想改位置：界面底部「📁 保存目录」→「选择目录」（重启后保持）
- exe 放在只读位置时自动回退到 `~/Music/2midi4lin/`

## 8. 一个完整流程示例

> 想把喜欢的歌变成 MIDI 并分享：

1. 转录页 → 拖入音频 → 选「翻奏改编」→ 开始转录
2. 完成后分享窗弹出 →「打开成品文件夹」找到 MIDI
3. 用林离打开 MIDI 播放 → 记下播放页面的分享码
4. 回软件填分享码 → 提交 → 作品上线

全程不到 3 分钟。

---

*遇到报错先看 [FAQ.md](FAQ.md)；更多项目信息见 README。*
