# 🚀 GPU 加速 — 内置，开箱即用

2midi4lin **内置 DirectML GPU 加速**（v0.2.1+），无需安装任何东西：
转录时自动检测显卡，能用 GPU 就用，不能用自动回退 CPU，功能完全一致。

---

## 你需要什么

| 项目 | 要求 |
|:----|:------|
| 操作系统 | **Windows 10 或 Windows 11**（64 位） |
| 显卡 | 任意支持 **DirectX 12** 的显卡（NVIDIA / AMD / Intel 核显均可） |
| 不需要 | ❌ 不需要安装 CUDA ❌ 不需要装显卡驱动 ❌ 不需要任何 Python 环境 |

> 绝大多数 Windows 电脑都满足条件。检测不到 GPU（虚拟机/远程桌面）时自动使用 CPU，不影响使用。

---

## 如何确认 GPU 加速生效

启动软件后，顶部状态条显示：

- **⚡ GPU 加速已启用（DirectML）** → 正在使用显卡加速
- **💻 使用 CPU 计算** → 本机未检测到可用 GPU（自动回退，功能正常）

> 检测逻辑：软件会实际验证显卡硬件 + 模型推理链路，不会出现「装了软件就显示 GPU」的误报。

---

## 速度能快多少？

**实测数据**（RTX 4080，v0.2.1 内置 DirectML）：

| 音频时长 | CPU | DirectML | 加速 |
|:--------|:---:|:--------:|:----:|
| 30 秒 | ~23 秒 | ~2.5 秒 | **9 倍** |
| 3 分钟（186 秒） | ~45 秒 | ~8 秒 | **5.7 倍** |

> 实际速度取决于你的 CPU 和显卡性能。无 GPU 的电脑使用 CPU 模式，速度与上表 CPU 列一致。

---

## 常见问题

### 显示「使用 CPU 计算」是坏了吗？

不是。说明本机没有检测到可用的硬件 GPU（例如虚拟机、远程桌面、显卡不支持 DX12）。
软件自动回退 CPU，转录功能完全正常，只是速度慢一些。

### 我的电脑有显卡但显示 CPU？

常见原因：
1. **显卡太老**，不支持 DirectX 12（需 DX12 才能用 DirectML）
2. **虚拟机 / 远程桌面**环境（微软远程显示适配器不算硬件 GPU）
3. 驱动异常（更新显卡驱动后重试）

### 笔记本有独显和核显，用哪个？

DirectML 会自动选择可用的 DX12 设备，通常用性能更强的独显。
